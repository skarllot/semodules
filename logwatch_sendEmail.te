
module logwatch_sendEmail 1.1;

require {
	type admin_home_t;
	type logwatch_t;
	class file { read execute open };
}

#============= logwatch_t ==============
# Allow access to sendEmail from logwatch (/usr/local/bin)
allow logwatch_t admin_home_t:file { read execute open };
